# Required: set this to your GitHub Codespace name so the frontend can reach the backend.
# Example: if your Codespace URL is https://my-codespace-8000.app.github.dev, set:
# VITE_CODESPACE_NAME=my-codespace
#
# Without this variable the app falls back to http://localhost:8000/api.
VITE_CODESPACE_NAME=
